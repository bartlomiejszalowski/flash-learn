export type FeatureType = {
  icon: JSX.Element;
  title: string;
  description: string;
};

export type TestimonialType = {
  name: string;
  avatar: string;
  role: string;
  content: string;
};

export type LearningEffectivenessItemType = {
  Icon: JSX.Element;
  label: string;
  description: string;
};
